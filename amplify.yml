version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npx ng version
    build:
      commands:
          - npm run build
  artifacts:
    baseDirectory: dist/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
